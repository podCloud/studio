import { StyleSheet } from "react-native";

export default StyleSheet.create({
  tab: {
    maxWidth: 220,
    height: "100%",
    padding: 0,
    marginHorizontal: 15,
    alignItems: "center",
    justifyContent: "center",
  },
  tabText: {
    paddingHorizontal: 5,
  },
});
